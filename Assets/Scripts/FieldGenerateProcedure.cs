
using UnityEngine;

public class FieldGenerateProcedure : MonoBehaviour
{
    // suppose this thing should collect FieldBlock data and Field data
    // Start is called before the first frame update
    void Start() {}
    
    void GenerateField() {}
}
